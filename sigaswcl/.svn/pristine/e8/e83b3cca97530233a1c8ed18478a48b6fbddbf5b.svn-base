import { TestBed, inject } from '@angular/core/testing';

import { AliquoteService } from './aliquote.service';

describe('AliquoteService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AliquoteService]
    });
  });

  it('should be created', inject([AliquoteService], (service: AliquoteService) => {
    expect(service).toBeTruthy();
  }));
});
