import { ImportazioneUtfRoutingModule } from './importazione-utf-routing.module';

describe('ImportazioneUtfRoutingModule', () => {
  let importazioneUtfRoutingModule: ImportazioneUtfRoutingModule;

  beforeEach(() => {
    importazioneUtfRoutingModule = new ImportazioneUtfRoutingModule();
  });

  it('should create an instance', () => {
    expect(importazioneUtfRoutingModule).toBeTruthy();
  });
});
